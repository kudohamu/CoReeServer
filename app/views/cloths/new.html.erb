<div class="clothsNew">
  <div class="clothsNew__Container">
    <div class="clothsNew__formContainer">
      <%= form_for @cloth do |f| %>
        <table>
          <tr>
            <th><%= f.label :name, "商品名：", class: "formLabel" %></th>
            <td><%= f.text_field :name %></td>
          </tr>
          <tr>
          </tr>
        </table>
      <% end %>
    </div>
  </div>
</div>
